[Unit]
Description=pi-netzero uplink monitor (ntfy ✅/⚠️ when the Pi gains/loses internet via the phone)
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/sbin/pitail-uplink-monitor.sh
Restart=always
RestartSec=5

[Install]
WantedBy=multi-user.target
